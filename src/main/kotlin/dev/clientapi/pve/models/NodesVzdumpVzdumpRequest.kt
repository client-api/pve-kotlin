/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "DuplicatedCode",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "RemoveRedundantCallsOfConversionMethods",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "RedundantUnitReturnType",
    "RemoveEmptyClassBody",
    "UnnecessaryVariable",
    "UnusedImport",
    "UnnecessaryVariable",
    "unused"
)

package dev.clientapi.pve.models

import dev.clientapi.pve.models.PveBoolean
import dev.clientapi.pve.models.PveClusterBackupModeEnum
import dev.clientapi.pve.models.PveCompressEnum
import dev.clientapi.pve.models.PveMailnotificationEnum
import dev.clientapi.pve.models.PveNotificationModeEnum
import dev.clientapi.pve.models.PvePbsChangeDetectionModeEnum

import com.squareup.moshi.Json
import com.squareup.moshi.JsonClass

/**
 * 
 *
 * @param all Backup all known guest systems on this host.
 * @param bwlimit Limit I/O bandwidth (in KiB/s).
 * @param compress Compress dump file.
 * @param dumpdir Store resulting files to specified directory.
 * @param exclude Exclude specified guest systems (assumes --all)
 * @param excludePath Exclude certain files/directories (shell globs). Paths starting with '/' are anchored to the container's root, other paths match relative to each subdirectory.
 * @param fleecing Options for backup fleecing (VM only).
 * @param ionice Set IO priority when using the BFQ scheduler. For snapshot and suspend mode backups of VMs, this only affects the compressor. A value of 8 means the idle priority is used, otherwise the best-effort priority is used with the specified value.
 * @param jobId The ID of the backup job. If set, the 'backup-job' metadata field of the backup notification will be set to this value. Only root@pam can set this parameter.
 * @param lockwait Maximal time to wait for the global lock (minutes).
 * @param mailnotification Deprecated: use notification targets/matchers instead. Specify when to send a notification mail
 * @param mailto Deprecated: Use notification targets/matchers instead. Comma-separated list of email addresses or users that should receive email notifications.
 * @param maxfiles Deprecated: use 'prune-backups' instead. Maximal number of backup files per guest system.
 * @param mode Backup mode.
 * @param notesTemplate Template string for generating notes for the backup(s). It can contain variables which will be replaced by their values. Currently supported are {{cluster}}, {{guestname}}, {{node}}, and {{vmid}}, but more might be added in the future. Needs to be a single line, newline and backslash need to be escaped as '\\n' and '\\\\' respectively.
 * @param notificationMode Determine which notification system to use. If set to 'legacy-sendmail', vzdump will consider the mailto/mailnotification parameters and send emails to the specified address(es) via the 'sendmail' command. If set to 'notification-system', a notification will be sent via PVE's notification system, and the mailto and mailnotification will be ignored. If set to 'auto' (default setting), an email will be sent if mailto is set, and the notification system will be used if not.
 * @param pbsChangeDetectionMode PBS mode used to detect file changes and switch encoding format for container backups.
 * @param performance Other performance-related settings.
 * @param pigz Use pigz instead of gzip when N>0. N=1 uses half of cores, N>1 uses N as thread count.
 * @param pool Backup all known guest systems included in the specified pool.
 * @param `protected` If true, mark backup(s) as protected.
 * @param pruneBackups Use these retention options instead of those from the storage configuration.
 * @param quiet Be quiet.
 * @param remove Prune older backups according to 'prune-backups'.
 * @param script Use specified hook script.
 * @param stdexcludes Exclude temporary files and logs.
 * @param stdout Write tar to stdout, not to a file.
 * @param stop Stop running backup jobs on this host.
 * @param stopwait Maximal time to wait until a guest system is stopped (minutes).
 * @param storage Store resulting file to this storage.
 * @param tmpdir Store temporary files to specified directory.
 * @param vmid The ID of the guest system you want to backup.
 * @param zstd Zstd threads. N=0 uses half of the available cores, if N is set to a value bigger than 0, N is used as thread count.
 */


data class NodesVzdumpVzdumpRequest (

    /* Backup all known guest systems on this host. */
    @Json(name = "all")
    val all: PveBoolean? = null,

    /* Limit I/O bandwidth (in KiB/s). */
    @Json(name = "bwlimit")
    val bwlimit: kotlin.Long? = 0L,

    /* Compress dump file. */
    @Json(name = "compress")
    val compress: PveCompressEnum? = null,

    /* Store resulting files to specified directory. */
    @Json(name = "dumpdir")
    val dumpdir: kotlin.String? = null,

    /* Exclude specified guest systems (assumes --all) */
    @Json(name = "exclude")
    val exclude: kotlin.String? = null,

    /* Exclude certain files/directories (shell globs). Paths starting with '/' are anchored to the container's root, other paths match relative to each subdirectory. */
    @Json(name = "exclude-path")
    val excludePath: kotlin.collections.List<kotlin.String>? = null,

    /* Options for backup fleecing (VM only). */
    @Json(name = "fleecing")
    val fleecing: kotlin.String? = null,

    /* Set IO priority when using the BFQ scheduler. For snapshot and suspend mode backups of VMs, this only affects the compressor. A value of 8 means the idle priority is used, otherwise the best-effort priority is used with the specified value. */
    @Json(name = "ionice")
    val ionice: kotlin.Int? = 7,

    /* The ID of the backup job. If set, the 'backup-job' metadata field of the backup notification will be set to this value. Only root@pam can set this parameter. */
    @Json(name = "job-id")
    val jobId: kotlin.String? = null,

    /* Maximal time to wait for the global lock (minutes). */
    @Json(name = "lockwait")
    val lockwait: kotlin.Long? = 180L,

    /* Deprecated: use notification targets/matchers instead. Specify when to send a notification mail */
    @Json(name = "mailnotification")
    val mailnotification: PveMailnotificationEnum? = null,

    /* Deprecated: Use notification targets/matchers instead. Comma-separated list of email addresses or users that should receive email notifications. */
    @Json(name = "mailto")
    val mailto: kotlin.String? = null,

    /* Deprecated: use 'prune-backups' instead. Maximal number of backup files per guest system. */
    @Json(name = "maxfiles")
    val maxfiles: kotlin.Long? = null,

    /* Backup mode. */
    @Json(name = "mode")
    val mode: PveClusterBackupModeEnum? = null,

    /* Template string for generating notes for the backup(s). It can contain variables which will be replaced by their values. Currently supported are {{cluster}}, {{guestname}}, {{node}}, and {{vmid}}, but more might be added in the future. Needs to be a single line, newline and backslash need to be escaped as '\\n' and '\\\\' respectively. */
    @Json(name = "notes-template")
    val notesTemplate: kotlin.String? = null,

    /* Determine which notification system to use. If set to 'legacy-sendmail', vzdump will consider the mailto/mailnotification parameters and send emails to the specified address(es) via the 'sendmail' command. If set to 'notification-system', a notification will be sent via PVE's notification system, and the mailto and mailnotification will be ignored. If set to 'auto' (default setting), an email will be sent if mailto is set, and the notification system will be used if not. */
    @Json(name = "notification-mode")
    val notificationMode: PveNotificationModeEnum? = null,

    /* PBS mode used to detect file changes and switch encoding format for container backups. */
    @Json(name = "pbs-change-detection-mode")
    val pbsChangeDetectionMode: PvePbsChangeDetectionModeEnum? = null,

    /* Other performance-related settings. */
    @Json(name = "performance")
    val performance: kotlin.String? = null,

    /* Use pigz instead of gzip when N>0. N=1 uses half of cores, N>1 uses N as thread count. */
    @Json(name = "pigz")
    val pigz: kotlin.Long? = 0L,

    /* Backup all known guest systems included in the specified pool. */
    @Json(name = "pool")
    val pool: kotlin.String? = null,

    /* If true, mark backup(s) as protected. */
    @Json(name = "protected")
    val `protected`: PveBoolean? = null,

    /* Use these retention options instead of those from the storage configuration. */
    @Json(name = "prune-backups")
    val pruneBackups: kotlin.String? = "keep-all=1",

    /* Be quiet. */
    @Json(name = "quiet")
    val quiet: PveBoolean? = null,

    /* Prune older backups according to 'prune-backups'. */
    @Json(name = "remove")
    val remove: PveBoolean? = null,

    /* Use specified hook script. */
    @Json(name = "script")
    val script: kotlin.String? = null,

    /* Exclude temporary files and logs. */
    @Json(name = "stdexcludes")
    val stdexcludes: PveBoolean? = null,

    /* Write tar to stdout, not to a file. */
    @Json(name = "stdout")
    val stdout: PveBoolean? = null,

    /* Stop running backup jobs on this host. */
    @Json(name = "stop")
    val stop: PveBoolean? = null,

    /* Maximal time to wait until a guest system is stopped (minutes). */
    @Json(name = "stopwait")
    val stopwait: kotlin.Long? = 10L,

    /* Store resulting file to this storage. */
    @Json(name = "storage")
    val storage: kotlin.String? = null,

    /* Store temporary files to specified directory. */
    @Json(name = "tmpdir")
    val tmpdir: kotlin.String? = null,

    /* The ID of the guest system you want to backup. */
    @Json(name = "vmid")
    val vmid: kotlin.String? = null,

    /* Zstd threads. N=0 uses half of the available cores, if N is set to a value bigger than 0, N is used as thread count. */
    @Json(name = "zstd")
    val zstd: kotlin.Long? = 1L

) {


}

