/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "DuplicatedCode",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "RemoveRedundantCallsOfConversionMethods",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "RedundantUnitReturnType",
    "RemoveEmptyClassBody",
    "UnnecessaryVariable",
    "UnusedImport",
    "UnnecessaryVariable",
    "unused"
)

package dev.clientapi.pve.models

import dev.clientapi.pve.models.PveBoolean
import dev.clientapi.pve.models.PveBwlimitField
import dev.clientapi.pve.models.PveFormatFormatEnum
import dev.clientapi.pve.models.PvePreallocationEnum
import dev.clientapi.pve.models.PveSaferemoveStepsizeEnum
import dev.clientapi.pve.models.PveSmbversionEnum
import dev.clientapi.pve.models.PveStorageBtrfsConfig
import dev.clientapi.pve.models.PveStorageCephfsConfig
import dev.clientapi.pve.models.PveStorageCifsConfig
import dev.clientapi.pve.models.PveStorageDirConfig
import dev.clientapi.pve.models.PveStorageEsxiConfig
import dev.clientapi.pve.models.PveStorageIscsiConfig
import dev.clientapi.pve.models.PveStorageIscsidirectConfig
import dev.clientapi.pve.models.PveStorageLvmConfig
import dev.clientapi.pve.models.PveStorageLvmthinConfig
import dev.clientapi.pve.models.PveStorageNfsConfig
import dev.clientapi.pve.models.PveStoragePbsConfig
import dev.clientapi.pve.models.PveStorageRbdConfig
import dev.clientapi.pve.models.PveStorageZfsConfig
import dev.clientapi.pve.models.PveStorageZfspoolConfig

import com.squareup.moshi.Json
import com.squareup.moshi.JsonClass

/**
 * 
 *
 * @param path File system path.
 * @param type 
 * @param username RBD Id.
 * @param server Server IP or DNS name.
 * @param share CIFS share.
 * @param portal iSCSI portal (IP or DNS name with optional port).
 * @param target iSCSI target.
 * @param vgname Volume group name.
 * @param thinpool LVM thin pool LV name.
 * @param export NFS export path.
 * @param datastore Proxmox Backup Server datastore name.
 * @param pool Pool.
 * @param blocksize block size
 * @param iscsiprovider iscsi provider
 * @param nodes List of nodes for which the storage configuration applies.
 * @param shared Indicate that this is a single storage with the same contents on all nodes (or all listed in the 'nodes' option). It will not make the contents of a local storage automatically accessible to other nodes, it just marks an already shared storage as such!
 * @param disable Flag to disable the storage.
 * @param pruneBackups The retention options with shorter intervals are processed first with --keep-last being the very first one. Each option covers a specific period of time. We say that backups within this period are covered by this option. The next option does not take care of already covered backups and only considers older backups.
 * @param maxProtectedBackups Maximal number of protected backups per guest. Use '-1' for unlimited.
 * @param content Allowed content types.  NOTE: the value 'rootdir' is used for Containers, and value 'images' for VMs. 
 * @param format Default image format.
 * @param isMountpoint Assume the given path is an externally managed mountpoint and consider the storage offline if it is not mounted. Using a boolean (yes/no) value serves as a shortcut to using the target path in this field.
 * @param nocow Set the NOCOW flag on files. Disables data checksumming and causes data errors to be unrecoverable from while allowing direct I/O. Only use this if data does not need to be any more safe than on a single ext4 formatted disk with no underlying raid system.
 * @param mkdir Create the directory if it doesn't exist and populate it with default sub-dirs. NOTE: Deprecated, use the 'create-base-path' and 'create-subdirs' options instead.
 * @param createBasePath Create the base directory if it doesn't exist.
 * @param createSubdirs Populate the directory with the default structure.
 * @param preallocation Preallocation mode for raw and qcow2 images. Using 'metadata' on raw images results in preallocation=off.
 * @param contentDirs Overrides for default content type directories.
 * @param monhost IP addresses of monitors (for external clusters).
 * @param subdir Subdir to mount.
 * @param options NFS/CIFS mount options (see 'man nfs' or 'man mount.cifs')
 * @param fuse Mount CephFS through FUSE.
 * @param bwlimit Set I/O bandwidth limit for various operations (in KiB/s).
 * @param keyring Client keyring contents (for external clusters).
 * @param fsName The Ceph filesystem name.
 * @param password Password for accessing the share/datastore.
 * @param domain CIFS domain.
 * @param smbversion SMB protocol version. 'default' if not set, negotiates the highest SMB2+ version supported by both the client and server.
 * @param snapshotAsVolumeChain Enable support for creating storage-vendor agnostic snapshot through volume backing-chains.
 * @param skipCertVerification Disable TLS certificate verification, only enable on fully trusted networks!
 * @param port Use this port to connect to the storage instead of the default one (for example, with PBS or ESXi). For NFS and CIFS, use the 'options' option to configure the port via the mount options.
 * @param saferemove Zero-out data when removing LVs.
 * @param saferemoveStepsize Wipe step size in MiB. It will be capped to the maximum supported by the storage.
 * @param saferemoveThroughput Wipe throughput (cstream -t parameter value).
 * @param base Base volume. This volume is automatically activated.
 * @param taggedOnly Only use logical volumes tagged with 'pve-vm-ID'.
 * @param namespace Namespace.
 * @param encryptionKey Encryption key. Use 'autogen' to generate one automatically without passphrase.
 * @param masterPubkey Base64-encoded, PEM-formatted public RSA key. Used to encrypt a copy of the encryption-key which will be added to each encrypted backup.
 * @param fingerprint Certificate SHA 256 fingerprint.
 * @param dataPool Data Pool (for erasure coding only)
 * @param krbd Always access rbd through krbd kernel module.
 * @param nowritecache disable write caching on the target
 * @param sparse use sparse volumes
 * @param comstarHg host group for comstar views
 * @param comstarTg target group for comstar views
 * @param lioTpg target portal group for Linux LIO targets
 * @param zfsBasePath Base path where to look for the created ZFS block devices. Set automatically during creation if not specified. Usually '/dev/zvol'.
 * @param mountpoint mount point
 */


interface StorageCreateStorageRequest {

    /* File system path. */
    @Json(name = "path")
    val path: kotlin.String
    @Json(name = "type")
    val type: StorageCreateStorageRequest.Type
    /* RBD Id. */
    @Json(name = "username")
    val username: kotlin.String
    /* Server IP or DNS name. */
    @Json(name = "server")
    val server: kotlin.String
    /* CIFS share. */
    @Json(name = "share")
    val share: kotlin.String
    /* iSCSI portal (IP or DNS name with optional port). */
    @Json(name = "portal")
    val portal: kotlin.String
    /* iSCSI target. */
    @Json(name = "target")
    val target: kotlin.String
    /* Volume group name. */
    @Json(name = "vgname")
    val vgname: kotlin.String
    /* LVM thin pool LV name. */
    @Json(name = "thinpool")
    val thinpool: kotlin.String
    /* NFS export path. */
    @Json(name = "export")
    val export: kotlin.String
    /* Proxmox Backup Server datastore name. */
    @Json(name = "datastore")
    val datastore: kotlin.String
    /* Pool. */
    @Json(name = "pool")
    val pool: kotlin.String
    /* block size */
    @Json(name = "blocksize")
    val blocksize: kotlin.String
    /* iscsi provider */
    @Json(name = "iscsiprovider")
    val iscsiprovider: kotlin.String
    /* List of nodes for which the storage configuration applies. */
    @Json(name = "nodes")
    val nodes: kotlin.String?
    /* Indicate that this is a single storage with the same contents on all nodes (or all listed in the 'nodes' option). It will not make the contents of a local storage automatically accessible to other nodes, it just marks an already shared storage as such! */
    @Json(name = "shared")
    val shared: PveBoolean?
    /* Flag to disable the storage. */
    @Json(name = "disable")
    val disable: PveBoolean?
    /* The retention options with shorter intervals are processed first with --keep-last being the very first one. Each option covers a specific period of time. We say that backups within this period are covered by this option. The next option does not take care of already covered backups and only considers older backups. */
    @Json(name = "prune-backups")
    val pruneBackups: kotlin.String?
    /* Maximal number of protected backups per guest. Use '-1' for unlimited. */
    @Json(name = "max-protected-backups")
    val maxProtectedBackups: kotlin.Long?
    /* Allowed content types.  NOTE: the value 'rootdir' is used for Containers, and value 'images' for VMs.  */
    @Json(name = "content")
    val content: kotlin.String?
    /* Default image format. */
    @Json(name = "format")
    val format: PveFormatFormatEnum?
    /* Assume the given path is an externally managed mountpoint and consider the storage offline if it is not mounted. Using a boolean (yes/no) value serves as a shortcut to using the target path in this field. */
    @Json(name = "is_mountpoint")
    val isMountpoint: kotlin.String?
    /* Set the NOCOW flag on files. Disables data checksumming and causes data errors to be unrecoverable from while allowing direct I/O. Only use this if data does not need to be any more safe than on a single ext4 formatted disk with no underlying raid system. */
    @Json(name = "nocow")
    val nocow: PveBoolean?
    /* Create the directory if it doesn't exist and populate it with default sub-dirs. NOTE: Deprecated, use the 'create-base-path' and 'create-subdirs' options instead. */
    @Json(name = "mkdir")
    val mkdir: PveBoolean?
    /* Create the base directory if it doesn't exist. */
    @Json(name = "create-base-path")
    val createBasePath: PveBoolean?
    /* Populate the directory with the default structure. */
    @Json(name = "create-subdirs")
    val createSubdirs: PveBoolean?
    /* Preallocation mode for raw and qcow2 images. Using 'metadata' on raw images results in preallocation=off. */
    @Json(name = "preallocation")
    val preallocation: PvePreallocationEnum?
    /* Overrides for default content type directories. */
    @Json(name = "content-dirs")
    val contentDirs: kotlin.String?
    /* IP addresses of monitors (for external clusters). */
    @Json(name = "monhost")
    val monhost: kotlin.String?
    /* Subdir to mount. */
    @Json(name = "subdir")
    val subdir: kotlin.String?
    /* NFS/CIFS mount options (see 'man nfs' or 'man mount.cifs') */
    @Json(name = "options")
    val options: kotlin.String?
    /* Mount CephFS through FUSE. */
    @Json(name = "fuse")
    val fuse: PveBoolean?
    /* Set I/O bandwidth limit for various operations (in KiB/s). */
    @Json(name = "bwlimit")
    val bwlimit: PveBwlimitField?
    /* Client keyring contents (for external clusters). */
    @Json(name = "keyring")
    val keyring: kotlin.String?
    /* The Ceph filesystem name. */
    @Json(name = "fs-name")
    val fsName: kotlin.String?
    /* Password for accessing the share/datastore. */
    @Json(name = "password")
    val password: kotlin.String?
    /* CIFS domain. */
    @Json(name = "domain")
    val domain: kotlin.String?
    /* SMB protocol version. 'default' if not set, negotiates the highest SMB2+ version supported by both the client and server. */
    @Json(name = "smbversion")
    val smbversion: PveSmbversionEnum?
    /* Enable support for creating storage-vendor agnostic snapshot through volume backing-chains. */
    @Json(name = "snapshot-as-volume-chain")
    val snapshotAsVolumeChain: PveBoolean?
    /* Disable TLS certificate verification, only enable on fully trusted networks! */
    @Json(name = "skip-cert-verification")
    val skipCertVerification: PveBoolean?
    /* Use this port to connect to the storage instead of the default one (for example, with PBS or ESXi). For NFS and CIFS, use the 'options' option to configure the port via the mount options. */
    @Json(name = "port")
    val port: kotlin.Int?
    /* Zero-out data when removing LVs. */
    @Json(name = "saferemove")
    val saferemove: PveBoolean?
    /* Wipe step size in MiB. It will be capped to the maximum supported by the storage. */
    @Json(name = "saferemove-stepsize")
    val saferemoveStepsize: PveSaferemoveStepsizeEnum?
    /* Wipe throughput (cstream -t parameter value). */
    @Json(name = "saferemove_throughput")
    val saferemoveThroughput: kotlin.String?
    /* Base volume. This volume is automatically activated. */
    @Json(name = "base")
    val base: kotlin.String?
    /* Only use logical volumes tagged with 'pve-vm-ID'. */
    @Json(name = "tagged_only")
    val taggedOnly: PveBoolean?
    /* Namespace. */
    @Json(name = "namespace")
    val namespace: kotlin.String?
    /* Encryption key. Use 'autogen' to generate one automatically without passphrase. */
    @Json(name = "encryption-key")
    val encryptionKey: kotlin.String?
    /* Base64-encoded, PEM-formatted public RSA key. Used to encrypt a copy of the encryption-key which will be added to each encrypted backup. */
    @Json(name = "master-pubkey")
    val masterPubkey: kotlin.String?
    /* Certificate SHA 256 fingerprint. */
    @Json(name = "fingerprint")
    val fingerprint: kotlin.String?
    /* Data Pool (for erasure coding only) */
    @Json(name = "data-pool")
    val dataPool: kotlin.String?
    /* Always access rbd through krbd kernel module. */
    @Json(name = "krbd")
    val krbd: PveBoolean?
    /* disable write caching on the target */
    @Json(name = "nowritecache")
    val nowritecache: PveBoolean?
    /* use sparse volumes */
    @Json(name = "sparse")
    val sparse: PveBoolean?
    /* host group for comstar views */
    @Json(name = "comstar_hg")
    val comstarHg: kotlin.String?
    /* target group for comstar views */
    @Json(name = "comstar_tg")
    val comstarTg: kotlin.String?
    /* target portal group for Linux LIO targets */
    @Json(name = "lio_tpg")
    val lioTpg: kotlin.String?
    /* Base path where to look for the created ZFS block devices. Set automatically during creation if not specified. Usually '/dev/zvol'. */
    @Json(name = "zfs-base-path")
    val zfsBasePath: kotlin.String?
    /* mount point */
    @Json(name = "mountpoint")
    val mountpoint: kotlin.String?
    /**
     * 
     *
     * Values: btrfs,cephfs,cifs,dir,esxi,iscsi,iscsidirect,lvm,lvmthin,nfs,pbs,rbd,zfs,zfspool
     */
    @JsonClass(generateAdapter = false)
    enum class Type(val value: kotlin.String) {
        @Json(name = "btrfs") btrfs("btrfs"),
        @Json(name = "cephfs") cephfs("cephfs"),
        @Json(name = "cifs") cifs("cifs"),
        @Json(name = "dir") dir("dir"),
        @Json(name = "esxi") esxi("esxi"),
        @Json(name = "iscsi") iscsi("iscsi"),
        @Json(name = "iscsidirect") iscsidirect("iscsidirect"),
        @Json(name = "lvm") lvm("lvm"),
        @Json(name = "lvmthin") lvmthin("lvmthin"),
        @Json(name = "nfs") nfs("nfs"),
        @Json(name = "pbs") pbs("pbs"),
        @Json(name = "rbd") rbd("rbd"),
        @Json(name = "zfs") zfs("zfs"),
        @Json(name = "zfspool") zfspool("zfspool");
    }

}

