/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "DuplicatedCode",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "RemoveRedundantCallsOfConversionMethods",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "RedundantUnitReturnType",
    "RemoveEmptyClassBody",
    "UnnecessaryVariable",
    "UnusedImport",
    "UnnecessaryVariable",
    "unused"
)

package dev.clientapi.pve.models

import dev.clientapi.pve.models.PveBoolean
import dev.clientapi.pve.models.PveBwlimitField
import dev.clientapi.pve.models.PveClusterOptionsFencingEnum
import dev.clientapi.pve.models.PveClusterOptionsLanguageEnum
import dev.clientapi.pve.models.PveConsoleEnum
import dev.clientapi.pve.models.PveCrsField
import dev.clientapi.pve.models.PveHaField
import dev.clientapi.pve.models.PveKeyboardEnum
import dev.clientapi.pve.models.PveMigrationField
import dev.clientapi.pve.models.PveNextIdField
import dev.clientapi.pve.models.PveNotifyField
import dev.clientapi.pve.models.PveReplicationField
import dev.clientapi.pve.models.PveTagStyleField
import dev.clientapi.pve.models.PveU2fField
import dev.clientapi.pve.models.PveUserTagAccessField
import dev.clientapi.pve.models.PveWebauthnField

import com.squareup.moshi.Json
import com.squareup.moshi.JsonClass

/**
 * 
 *
 * @param bwlimit Set I/O bandwidth limit for various operations (in KiB/s).
 * @param consentText Consent text that is displayed before logging in.
 * @param console Select the default Console viewer. You can either use the builtin java applet (VNC; deprecated and maps to html5), an external virt-viewer comtatible application (SPICE), an HTML5 based vnc viewer (noVNC), or an HTML5 based console client (xtermjs). If the selected viewer is not available (e.g. SPICE not activated for the VM), the fallback is noVNC.
 * @param crs Cluster resource scheduling settings.
 * @param delete A list of settings you want to delete.
 * @param description Datacenter description. Shown in the web-interface datacenter notes panel. This is saved as comment inside the configuration file.
 * @param emailFrom Specify email address to send notification from (default is root@$hostname)
 * @param fencing Set the fencing mode of the HA cluster. Hardware mode needs a valid configuration of fence devices in /etc/pve/ha/fence.cfg. With both all two modes are used.  WARNING: 'hardware' and 'both' are EXPERIMENTAL & WIP
 * @param ha Cluster wide HA settings.
 * @param httpProxy Specify external http proxy which is used for downloads (example: 'http://username:password@host:port/')
 * @param keyboard Default keybord layout for vnc server.
 * @param language Default GUI language.
 * @param macPrefix Prefix for the auto-generated MAC addresses of virtual guests. The default `BC:24:11` is the Organizationally Unique Identifier (OUI) assigned by the IEEE to Proxmox Server Solutions GmbH for a MAC Address Block Large (MA-L). You're allowed to use this in local networks, i.e., those not directly reachable by the public (e.g., in a LAN or NAT/Masquerading).   Note that when you run multiple cluster that (partially) share the networks of their virtual guests, it's highly recommended that you extend the default MAC prefix, or generate a custom (valid) one, to reduce the chance of MAC collisions. For example, add a separate extra hexadecimal to the Proxmox OUI for each cluster, like `BC:24:11:0` for the first, `BC:24:11:1` for the second, and so on.  Alternatively, you can also separate the networks of the guests logically, e.g., by using VLANs.  For publicly accessible guests it's recommended that you get your own https://standards.ieee.org/products-programs/regauth/[OUI from the IEEE] registered or coordinate with your, or your hosting providers, network admins.
 * @param maxWorkers Defines how many workers (per node) are maximal started  on actions like 'stopall VMs' or task from the ha-manager.
 * @param migration For cluster wide migration settings.
 * @param migrationUnsecure Migration is secure using SSH tunnel by default. For secure private networks you can disable it to speed up migration. Deprecated, use the 'migration' property instead!
 * @param nextId Control the range for the free VMID auto-selection pool.
 * @param notify Cluster-wide notification settings.
 * @param registeredTags A list of tags that require a `Sys.Modify` on '/' to set and delete. Tags set here that are also in 'user-tag-access' also require `Sys.Modify`.
 * @param replication For cluster wide replication settings.
 * @param tagStyle Tag style options.
 * @param u2f u2f
 * @param userTagAccess Privilege options for user-settable tags
 * @param webauthn webauthn configuration
 */


data class ClusterOptionsSetOptionsRequest (

    /* Set I/O bandwidth limit for various operations (in KiB/s). */
    @Json(name = "bwlimit")
    val bwlimit: PveBwlimitField? = null,

    /* Consent text that is displayed before logging in. */
    @Json(name = "consent-text")
    val consentText: kotlin.String? = null,

    /* Select the default Console viewer. You can either use the builtin java applet (VNC; deprecated and maps to html5), an external virt-viewer comtatible application (SPICE), an HTML5 based vnc viewer (noVNC), or an HTML5 based console client (xtermjs). If the selected viewer is not available (e.g. SPICE not activated for the VM), the fallback is noVNC. */
    @Json(name = "console")
    val console: PveConsoleEnum? = null,

    /* Cluster resource scheduling settings. */
    @Json(name = "crs")
    val crs: PveCrsField? = null,

    /* A list of settings you want to delete. */
    @Json(name = "delete")
    val delete: kotlin.String? = null,

    /* Datacenter description. Shown in the web-interface datacenter notes panel. This is saved as comment inside the configuration file. */
    @Json(name = "description")
    val description: kotlin.String? = null,

    /* Specify email address to send notification from (default is root@$hostname) */
    @Json(name = "email_from")
    val emailFrom: kotlin.String? = null,

    /* Set the fencing mode of the HA cluster. Hardware mode needs a valid configuration of fence devices in /etc/pve/ha/fence.cfg. With both all two modes are used.  WARNING: 'hardware' and 'both' are EXPERIMENTAL & WIP */
    @Json(name = "fencing")
    val fencing: PveClusterOptionsFencingEnum? = null,

    /* Cluster wide HA settings. */
    @Json(name = "ha")
    val ha: PveHaField? = null,

    /* Specify external http proxy which is used for downloads (example: 'http://username:password@host:port/') */
    @Json(name = "http_proxy")
    val httpProxy: kotlin.String? = null,

    /* Default keybord layout for vnc server. */
    @Json(name = "keyboard")
    val keyboard: PveKeyboardEnum? = null,

    /* Default GUI language. */
    @Json(name = "language")
    val language: PveClusterOptionsLanguageEnum? = null,

    /* Prefix for the auto-generated MAC addresses of virtual guests. The default `BC:24:11` is the Organizationally Unique Identifier (OUI) assigned by the IEEE to Proxmox Server Solutions GmbH for a MAC Address Block Large (MA-L). You're allowed to use this in local networks, i.e., those not directly reachable by the public (e.g., in a LAN or NAT/Masquerading).   Note that when you run multiple cluster that (partially) share the networks of their virtual guests, it's highly recommended that you extend the default MAC prefix, or generate a custom (valid) one, to reduce the chance of MAC collisions. For example, add a separate extra hexadecimal to the Proxmox OUI for each cluster, like `BC:24:11:0` for the first, `BC:24:11:1` for the second, and so on.  Alternatively, you can also separate the networks of the guests logically, e.g., by using VLANs.  For publicly accessible guests it's recommended that you get your own https://standards.ieee.org/products-programs/regauth/[OUI from the IEEE] registered or coordinate with your, or your hosting providers, network admins. */
    @Json(name = "mac_prefix")
    val macPrefix: kotlin.String? = "BC:24:11",

    /* Defines how many workers (per node) are maximal started  on actions like 'stopall VMs' or task from the ha-manager. */
    @Json(name = "max_workers")
    val maxWorkers: kotlin.Long? = null,

    /* For cluster wide migration settings. */
    @Json(name = "migration")
    val migration: PveMigrationField? = null,

    /* Migration is secure using SSH tunnel by default. For secure private networks you can disable it to speed up migration. Deprecated, use the 'migration' property instead! */
    @Json(name = "migration_unsecure")
    val migrationUnsecure: PveBoolean? = null,

    /* Control the range for the free VMID auto-selection pool. */
    @Json(name = "next-id")
    val nextId: PveNextIdField? = null,

    /* Cluster-wide notification settings. */
    @Json(name = "notify")
    val notify: PveNotifyField? = null,

    /* A list of tags that require a `Sys.Modify` on '/' to set and delete. Tags set here that are also in 'user-tag-access' also require `Sys.Modify`. */
    @Json(name = "registered-tags")
    val registeredTags: kotlin.String? = null,

    /* For cluster wide replication settings. */
    @Json(name = "replication")
    val replication: PveReplicationField? = null,

    /* Tag style options. */
    @Json(name = "tag-style")
    val tagStyle: PveTagStyleField? = null,

    /* u2f */
    @Json(name = "u2f")
    val u2f: PveU2fField? = null,

    /* Privilege options for user-settable tags */
    @Json(name = "user-tag-access")
    val userTagAccess: PveUserTagAccessField? = null,

    /* webauthn configuration */
    @Json(name = "webauthn")
    val webauthn: PveWebauthnField? = null

) {


}

